// TODO
// rebuild-index pattern doesn't play well with @nozbe/watermelondb/adapters/sqlite - it tries to import better-sqlite automatically
// web and app both need to be able to import *either* sqlite or lokijs declaratively, like `import from 'musora-content-services/sync/adapters/sqlite'
export const SQLiteAdapter = {};
