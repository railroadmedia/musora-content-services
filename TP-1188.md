## Summary
| Tier | Count | Files |
|---|---|---|
| None / Pure (no I/O) | ~220 | lib/query.test.ts, lib/filter.test.ts, imageSRCBuilder, imageSRCVerify, dateUtils, HttpClient, award-calculations, message-generator |
| Mocked repo-proxy / HTTP (CI-safe integration) | ~130 | awards/ (12 files), contentProgress, contentLikes, userActivity, notifications, progressRows, streakMessage, dataContext, lastUpdated, sync/models/award-database-integration |
| Real Sanity CMS (currently skipped) | ~65 | sanityQueryService (Sanity Queries, api.v1, api.v1.admin, fetchAllFilterOptions* blocks), content.test.js, contentProgress (get-Songs-*) |
| Real Railcontent API (currently skipped) | ~3 | forum.test.js, user/permissions.test.js, sanityQueryService (Recommended System) |
| Real Sanity + Railcontent, destructive (currently skipped) | ~2 | learningPaths.test.js, sanityQueryService (Recommended System) |
## Key findings
1. All currently-skipped tests require real Sanity CMS or Railcontent credentials — none are skipped due to logic issues (those were fixed in PR #908).
2. content.test.js and learningPaths.test.js both call initializeTestService(true) (real HTTP login) in beforeAll/beforeEach even though every test is skipped — wasted network call on every CI run.
3. No currently-passing test makes a real network call — CI is clean.
4. test/live/ contains one stub file (zero tests) and one real file (contentProgressLive.test.js) with destructive write tests against a real user account.
## Next step
TP-1189 — implement the three-tier directory structure (unit / integration / live) and move files accordingly.
